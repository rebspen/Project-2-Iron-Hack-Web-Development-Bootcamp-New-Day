<div class="mainContainer">

  <div class="title"><h1>New Day</h1></div>
  <p> <span></span>The daily 5 minute journal you'll actually stick with.... </span></p>
{{!-- 
<button type="button" class="btn">
<a href= "/sign-in">Sign in</a>
<br>
<button type="button" class="btn"><a href = "/sign-up">Sign up</a></button>
<br> --}}

  <a href="/sign-in">Sign In</a>
  <a class = "signup" href="/sign-up">Sign Up</a>
  <br>
<div class = "affirm" ><p>Daily community affirmation...</p><br><p>"{{result.affirmation}}"</p></div>
<div id="map"></div>
<div id = "locations" ><p>{{result.location}}</p></div>

</div>

<script>
 let geocoder;
 let $location = document.getElementById('locations').innerText
 let city = $location

function initialize() 
{
  geocoder = new google.maps.Geocoder();
  var latlng = new google.maps.LatLng(53.3496, -6.3263);
  var mapOptions = 
  {
    zoom: 2,
    center: latlng
  }
  map = new google.maps.Map(document.getElementById('map'), mapOptions);
  codeAddress(city);

  
}

function codeAddress(address) 
{
  geocoder.geocode( {address:address}, function(results, status) 
  {
    if (status == google.maps.GeocoderStatus.OK) 
    {
      map.setCenter(results[0].geometry.location);//center the map over the result
      //place a marker at the location
      var marker = new google.maps.Marker(
      {
          map: map,
          position: results[0].geometry.location
      });
    } else {
      alert('Geocode was not successful for the following reason: ' + status);
   }
  });
}
</script>
